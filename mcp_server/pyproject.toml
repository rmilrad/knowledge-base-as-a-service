[project]
name = "kbaas-mcp"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
